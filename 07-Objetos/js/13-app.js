const producto = {
    nombre: "Monitor 27 Pulgadas",
    precio: 500,
    disponible: true,
    
}
// Te devuelve el el nombre de variable dentro de objeto
console.log(Object.keys(producto));

// Te devuelve el valor de la variable dentro de objeto
console.log(Object.values(producto));

// Te devuelve el nombre u el valor de la variable dentro de objeto en pares
console.log(Object.entries(producto));


